import React from 'react'
import Form from './Form'

const ApplicationForm = (props) => {
    return (
        <div className="container">
            <h2 className="my-5">Apply for job</h2>
            <Form/>
        </div>
    )
}

export default ApplicationForm